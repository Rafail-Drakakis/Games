package model.Square;

import model.Color.Color;

/**
 * Represents a square at the end of a slide.
 */
public class EndSlideSquare extends SlideSquare {
    /**
     * Constructs an EndSlideSquare object with the specified color.
     *
     * @param color The color of the EndSlideSquare.
     */
    public EndSlideSquare(Color color) {
        super(color);
    }

}